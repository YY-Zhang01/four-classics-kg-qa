# RedDream 项目未来发展规划

> 📌 说明：本文是**后续演进**规划（六阶段路线）。截至当前，安全与工程加固已全部完成
> （P0 安全修复 / P1 并发·SSE·认证·日志 / P2 Cypher·连接复用·死代码·测试），详见
> [README.md](./README.md) 与 git 提交历史。本文其余规划仍为后续方向参考。

## 1. 项目定位

### 1.1 当前定位

`RedDream` 当前定位为：

> 面向中国古典文学的企业级多领域智能问答系统，支持 KG+向量融合检索、Wiki 百科、Neo4j 图谱存储和 Agent 工具调用。

项目已经建立从 PDF 解析到 Web 流式问答的完整链路，并在红楼梦、三国演义、水浒传、西游记四个领域实现了多领域切换。当前处于 **P2→P3 过渡阶段**——知识图谱已建立但质量待提升，Agent 已跑通但能力有限。

### 1.2 未来定位

项目未来可以逐步升级为：

> 面向多领域文档的通用知识问答与智能分析平台，支持任意 PDF/文档的零代码接入、自动知识抽取、图谱构建、混合检索、Agent 自主推理和可视化分析。

当前架构已经为多领域扩展打下良好基础（domain 热切换、可插拔检索器、JWT 鉴权），后续演进的核心是**从"能跑"到"可靠"再到"智能"**。

```text
评测体系加固 → KG 质量提升 → Agent 能力升级
→ 工程化完善(Docker/CI) → 多模态支持 → 通用化平台
```

---

## 2. 当前项目基础

### 2.1 已具备能力

| 层次 | 能力 | 成熟度 |
|------|------|--------|
| **文档处理** | PDF → MinerU 解析 → Markdown → jieba 分词切块 | ★★★★ 稳定 |
| **向量化** | bge-m3 语义向量 + pgvector 存储 + 余弦相似度检索 | ★★★★ 稳定 |
| **知识图谱** | LLM 三元组抽取 + Schema 白名单校验 + Neo4j 存储 | ★★★ 可用，质量待提升 |
| **混合检索** | 关键词 / 向量 / KG / Wiki / Neo4j 五路融合 + 策略路由 | ★★★★ 核心亮点 |
| **Wiki 百科** | 人物/事项结构化百科页面，自动查询+手工编辑 | ★★★ 可用，依赖种子数据 |
| **Agent 系统** | ReAct 循环 + 工具注册表 + 纯文本解析 | ★★★ 基础跑通 |
| **多领域切换** | 运行时热切书籍 + 用户端/管理端独立状态 | ★★★★ 核心亮点 |
| **Web 界面** | 双角色 Web（用户聊天 + 管理后台）+ SSE 流式 | ★★★★ 可用 |
| **用户系统** | JWT 注册/登录 + 角色鉴权（user/admin） | ★★★ 基础可用 |
| **评测体系** | 12 题评测集 + 检索命中率 + MRR + LLM 裁判打分 | ★★★ 有框架，题目少 |
| **审计日志** | qa_audit.jsonl 全量记录问答 | ★★★★ 稳定 |
| **多轮对话** | 代词消解 + 上下文感知 | ★★★ 基础可用 |

### 2.2 当前主要问题

#### 架构层面
- **无 Docker 部署**：整套系统靠本地 venv + 手动启动多个服务（PostgreSQL、Neo4j、Ollama），新人接手门槛极高
- **无 CI/CD**：没有自动化测试流水线，改代码后靠人工跑测试
- **全局状态管理**：领域切换用模块级全局变量，并发场景下存在状态污染风险
- **web/server.py 已 1081 行**：单文件承载所有 API 路由，拆分为独立路由模块
- **缺少配置文件模板**：没有 `.env.example`，接手者不知道需要配哪些环境变量

#### KG 层面
- **三元组质量不稳定**：LLM 抽取结果缺乏质量评分，可能产生无意义关系
- **Schema 简陋**：只有关系白名单，没有实体类型体系、没有关系方向约束
- **人工种子数据依赖**：Wiki 百科和实体词典需要手工准备，迁移新领域成本高
- **Neo4j 是可选依赖**：未配置时静默降级到 PG，但两层数据可能不一致
- **缺少实体对齐**：同一人物在不同章节的称呼不同（如"宝玉""宝二爷""贾宝玉"），没有同义词合并

#### Agent 层面
- **纯文本解析脆弱**：靠正则匹配 `TOOL: xxx` 和 `ARGS: {...}`，LLM 格式偏差就会导致解析失败
- **工具数量少**：当前只有 search_knowledge、query_graph、query_wiki、compare_entities 四个工具
- **无思考链展示**：Agent 推理过程不透明，用户看不到它调用了什么工具、得到了什么结果
- **死循环保护粗暴**：最多 5 轮就截断，不能优雅收尾

#### 评测层面
- **评测集只有 12 题**：覆盖面太窄，且全是红楼梦领域
- **检索评测依赖关键词字符串匹配**：不准确，"巡盐御史"可能在文中以其他形式出现
- **LLM 裁判无校准**：打分标准主观，没有人工标注的基准分做对比

#### 工程化层面
- **依赖本地 Ollama**：部署到服务器需要额外处理 GPU 和模型下载
- **无请求缓存**：相同问题重复调用 LLM，浪费 token
- **无性能监控**：没有检索耗时、生成耗时、token 用量等统计面板（只有评测时手动计时）
- **测试覆盖不足**：只有少量单元测试，没有端到端测试

---

## 3. 总体建设路线

RedDream 已具备核心检索和问答能力，后续优化不应推倒重来，而是在现有架构上逐层加固。按六个阶段推进：

1. **阶段一：补齐工程化短板** — Docker、CI、配置标准化
2. **阶段二：KG 质量体系建设** — Schema 升级、实体对齐、质量评分
3. **阶段三：Agent 能力升级** — 工具函数式调用、思考链可视化
4. **阶段四：评测与迭代闭环** — 评测集扩展、自动化回归
5. **阶段五：多模态与性能优化** — 图片/表格支持、缓存、性能监控
6. **阶段六：通用化平台** — 零代码接入新领域、配置驱动建库

---

## 4. 阶段一：补齐工程化短板

### 4.1 预计时间

2～3 周。

### 4.2 建设目标

让项目能被别人（包括半年后的自己）快速跑起来，从"在我的机器上能跑"变成"有 Docker 就能跑"。

### 4.3 主要任务

#### 4.3.1 Docker Compose 编排

当前 RedDream 依赖三个外部服务：PostgreSQL、Neo4j、Ollama（可选）。需要提供一键启动的 Docker Compose 配置。

```yaml
# 建议的 services 拆分
services:
  postgres:    # PostgreSQL 18 + pgvector
  neo4j:       # Neo4j 5.x 社区版
  ollama:      # （可选）本地模型服务
  redis:       # （可选）缓存层，阶段五用
  app:         # FastAPI 应用（可先不容器化，或提供 Dockerfile）
```

#### 4.3.2 配置标准化

- 创建 `config/.env.example` 模板文件，注释说明每个变量的含义
- 创建 `scripts/setup.sh`（Windows 用 `scripts/setup.ps1`），自动检查前置依赖
- 创建 `scripts/quickstart.sh`，一键完成：环境检查 → 拉 Docker 镜像 → 建库 → 灌种子数据 → 启动

#### 4.3.3 web/server.py 拆分

当前 1081 行的单文件需要拆分为路由模块：

```text
web/
├── server.py          # 应用入口（~50 行）
├── routers/
│   ├── chat.py        # /api/ask 问答接口
│   ├── admin.py       # /api/admin/* 管理接口
│   ├── auth.py        # /api/auth/* 认证接口
│   ├── config.py      # /api/config 配置接口
│   └── health.py      # /api/health 健康检查
```

#### 4.3.4 CI 流水线

- 配置 GitLab CI（`.gitlab-ci.yml`）
- 自动运行：语法检查 → 单元测试 → 评测脚本（--fast 模式）
- 不做自动部署，只做质量门禁

#### 4.3.5 全局状态改为请求级上下文

当前领域切换用模块级 `_active_domain` 全局变量，改成 FastAPI 的 `request.state` 或中间件注入：

```python
# 改造方向：从全局变量 → 请求级上下文
# 用户端：每个请求根据用户 cookie/session 取 domain
# 管理端：每个请求根据 admin JWT 取 domain
```

### 4.4 验收标准

- `docker compose up` 一键启动全部服务，浏览器打开 `localhost:7860` 即可对话
- `config/.env.example` 文件存在且注释清晰
- `web/server.py` 不超过 100 行
- CI 流水线在 MR 时自动运行并通过
- 同一台机器上两个浏览器窗口切换不同书籍不相互干扰

### 4.5 阶段成果

> RedDream 变成一个标准化的、可部署的、团队可协作的项目。

---

## 5. 阶段二：KG 质量体系建设

### 5.1 预计时间

3～4 周。

### 5.2 建设目标

将当前"能用但粗糙"的知识图谱升级为"可信且规范"的知识图谱。

### 5.3 实体类型体系 V1

当前只有"人物"一类实体（通过 `entities.json` 的 aliases 定义）。建议扩展到：

| 实体类型 | 说明 | 示例（红楼梦） |
|---------|------|---------------|
| `person` | 人物 | 贾宝玉、林黛玉 |
| `location` | 地点 | 大观园、潇湘馆 |
| `object` | 物品 | 通灵宝玉、金锁 |
| `event` | 事件 | 元春省亲、抄检大观园 |
| `organization` | 家族/组织 | 贾府、荣国府 |
| `title` | 官职/称谓 | 巡盐御史、诰命夫人 |

### 5.4 关系类型升级

当前关系白名单偏简单（亲属、居住、官职），建议按实体类型交叉矩阵扩展：

| 主语类型 | 谓语 | 宾语类型 | 示例 |
|---------|------|---------|------|
| person | 亲属_of | person | 贾宝玉 亲属_of 贾政 |
| person | 爱慕 | person | 贾宝玉 爱慕 林黛玉 |
| person | 居住于 | location | 林黛玉 居住于 潇湘馆 |
| person | 担任 | title | 林如海 担任 巡盐御史 |
| person | 拥有 | object | 贾宝玉 拥有 通灵宝玉 |
| person | 参与 | event | 贾宝玉 参与 元春省亲 |
| organization | 包含 | location | 贾府 包含 大观园 |
| organization | 包含 | person | 贾府 包含 贾宝玉 |

### 5.5 实体对齐

同一个实体在文中可能有多种称呼，需要自动合并：

```text
原始抽取 → 名称规范化 → 同义词字典匹配 → 向量相似度 → 人工审核
```

- 名称规范化：去掉"贾""林"等姓氏前缀差异、去掉敬称
- 同义词字典：维护 `config/entity_aliases.json`，人工标注等价实体
- 向量相似度：用 bge-m3 计算候选实体向量距离，阈值以下自动合并

### 5.6 KG 质量评分

自动统计并展示：

- 实体类型数量（目标 ≥ 4 类）
- 关系类型数量（目标 ≥ 8 类）
- 孤立实体比例（目标 < 15%）
- LLM 抽取的三元组中，通过白名单校验的比例
- 每个实体的平均关系数（目标 ≥ 2）
- Neo4j 和 PG 的数据一致性检查

### 5.7 阶段成果

> RedDream 的知识图谱从"小说人物关系网"升级为"多类型、可推理、可验证的文学知识库"。

---

## 6. 阶段三：Agent 能力升级

### 6.1 预计时间

2～3 周。

### 6.2 建设目标

将当前脆弱的文本解析 Agent 升级为可靠的、可观测的、能力丰富的智能助手。

### 6.3 从文本解析到结构化调用

当前 Agent 靠正则匹配 `TOOL: xxx\nARGS: {...}` 来解析 LLM 的工具调用意图，格式稍偏就会失败。

改为**两步法**：

1. 如果 LLM 接口支持原生 function calling（OpenAI 兼容），直接用 JSON Schema 定义工具
2. 如果不支持，保留文本解析作为 fallback，但增强容错（JSON 修复、重试机制）

```python
# 改造方向：agent/base.py 中增加 function-calling 适配
class AgentBase:
    def _call_tool(self, response: str) -> ToolResult:
        # 优先解析 function_call 字段
        # 回退到文本 TOOL/ARGS 解析
        # 解析失败则重试一次
```

### 6.4 思考链可视化

每轮 Agent 对话应该让用户看到它的推理过程：

```text
用户：贾宝玉和林黛玉什么关系？

Agent 思考中...
├─ 🔍 调用 query_graph("贾宝玉 林黛玉")
│  └─ 返回 3 条关系记录
├─ 📖 调用 query_wiki("贾宝玉")
│  └─ 返回人物百科卡片
└─ 🤔 综合分析中...

回答：贾宝玉和林黛玉是知己关系，两人互生情愫...
```

前端 SSE 流中增加 `type: "agent_step"` 事件，展示每步工具调用和结果。

### 6.5 新增工具

| 工具 | 描述 | 优先级 |
|------|------|--------|
| `list_chapters` | 列出书籍章节目录 | P1 |
| `search_chapter` | 按章节范围检索原文 | P1 |
| `compare_events` | 对比两个事件的异同 | P2 |
| `timeline` | 查询人物/事件的时间线 | P2 |
| `relation_path` | 查询两个实体间的关系路径（Neo4j 多跳） | P1 |
| `count_mentions` | 统计某实体在某章节区间的出现频率 | P2 |

### 6.6 阶段成果

> Agent 能够自主选择工具、展示推理过程、回溯每一步的证据来源。

---

## 7. 阶段四：评测与迭代闭环

### 7.1 预计时间

2 周。

### 7.2 建设目标

建立可持续的评测-改进循环，每次改检索策略或 Prompt 后能快速验证效果。

### 7.3 评测集扩展

当前 12 题全是红楼梦。需要：

- 扩展到 50 题（每个领域至少 10 题）
- 增加问题类型：事实查询、关系推理、跨章节综合、对比分析、否命题
- 增加难度等级标记（简单/中等/困难）
- 标注每道题的预期检索来源（知道答案应该从哪个 chunk 来）

### 7.4 LLM 裁判校准

当前 LLM 裁判直接打分 1-5，缺少基准对比：

- 选取 10 题，人工标注"黄金标准答案"和期望分数
- 每次改 Prompt 后跑裁判，对比人工基准分
- 如果裁判分偏离基准超过 1 分，说明裁判本身需要调

### 7.5 回归评测脚本

```bash
# 改完代码后一键评测
python -m eval.run_eval --all-domains  # 跨所有领域评测
python -m eval.run_eval --compare main  # 对比当前分支和 main 分支的分数
```

输出差异报告：哪些题变好了、哪些题变差了、哪些新增问题类型漏了。

### 7.6 阶段成果

> 形成一个 50 题的跨领域评测集，每次改动都有数据支撑的决策依据。

---

## 8. 阶段五：多模态与性能优化

### 8.1 预计时间

2～3 周。

### 8.2 建设目标

让 RedDream 支持图片/表格内容的理解，同时优化响应速度和资源消耗。

### 8.3 图片理解

当前 RedDream 仅处理纯文本。古典文学 PDF 中常有插画、人物绣像、印章等图像内容：

- 利用 MinerU 已有的图片提取能力（`parsed/` 目录下有 images 文件夹）
- 接入多模态模型（如 qwen-vl）对图片生成文字描述
- 在检索结果中关联图片，前端展示时附带缩略图

### 8.4 表格/结构解析

- 人物关系表（如金陵十二钗排名表）、官职品级表等结构化内容
- 从 Markdown 表格自动转为结构化 JSON
- 支持按列查询（如"谁是一品诰命夫人？"）

### 8.5 性能优化

| 优化项 | 方案 | 预期效果 |
|--------|------|---------|
| 向量缓存 | Redis 缓存热点查询的 embedding | 重复问题零延迟 |
| LLM 响应缓存 | 相同问题 + 相同检索结果 → 直接返回缓存 | 节省 token |
| 切块策略优化 | 按章节语义边界切分（而非机械 500 字） | 检索精度提升 |
| 懒加载模块 | wiki、neo4j 等模块延迟初始化 | 冷启动提速 |
| 并发检索 | 向量/KG/Wiki 三路检索并发执行 | 检索耗时减半 |

### 8.6 性能监控面板

管理后台增加：

- 平均检索耗时（按检索方式分类）
- 平均生成耗时
- LLM Token 用量趋势
- 缓存命中率
- 问答总量/QPS

### 8.7 阶段成果

> RedDream 从"能回答问题"升级为"快、准、省"的高性能问答系统。

---

## 9. 阶段六：通用化平台

### 9.1 预计时间

3～4 周。

### 9.2 建设目标

从"四个古典小说专有系统"变为"任意 PDF 丢进去就能跑的知识问答平台"。

### 9.3 零代码接入新领域

当前接入新领域需要：准备 PDF → 跑 MinerU → 切块 → 向量化 → 写实体词典 → 灌 KG 种子数据。每一步都要手动操作。

改造为配置驱动的自动化流水线：

```text
用户上传 PDF → 系统自动解析 → 自动切块 → 自动向量化
→ LLM 自动发现实体 → 自动生成 KG Schema → 自动灌入 Neo4j
→ 自动生成评测题 → 自动跑评测 → 达标后自动上线
```

### 9.4 领域配置化

```json
// config/domain_template.json — 新领域只需要填这个
{
  "domain": "水浒传",
  "pdf_path": "pdfs/水浒传.pdf",
  "kg_schema": {
    "entity_types": ["person", "location", "event", "organization"],
    "relations": ["亲属", "结拜", "隶属", "敌对", "居住于"]
  },
  "auto_discover": {
    "entities": true,   // 自动从原文发现高频人名/地名
    "relations": true   // 自动推荐关系类型
  }
}
```

### 9.5 前端上传入口

管理后台增加"新建领域"页面：

1. 上传 PDF / 拖入文件
2. 系统自动解析并展示进度
3. 自动发现的实体/关系供用户审核、修改
4. 一键上线

### 9.6 阶段成果

> RedDream 成为一个真正的平台——不需要改代码，只需上传文档就能建立新领域的知识问答系统。

---

## 10. 建议时间表

| 时间 | 阶段 | 主要目标 | 可展示成果 |
|------|------|---------|-----------|
| 第 1～3 周 | 工程化 | Docker 部署 + CI + 代码整理 | 一键启动的开发环境 |
| 第 4～7 周 | KG 质量 | Schema 升级 + 实体对齐 + 质量评分 | 四类实体以上的知识图谱 |
| 第 8～10 周 | Agent | 结构化调用 + 思考链 + 新工具 | 能推理、能展示过程的 Agent |
| 第 11～12 周 | 评测 | 50 题评测集 + 自动化回归 | 每次改动有数据说话 |
| 第 13～15 周 | 多模态 | 图片理解 + 缓存 + 性能监控 | 快一倍的响应 + 支持图片 |
| 第 16～19 周 | 通用化 | 零代码接入 + 前端上传 | 丢 PDF 进去就出知识问答 |

按一人主导开发估算，实际时间取决于 Ollama 模型可用性、Neo4j 稳定性、PDF 文档质量。

---

## 11. 当前优先任务

### 11.1 写 .env.example

当前项目没有配置模板，接手者无从下手。这是**最优先、最简单的改进**。

```text
config/.env.example   ← 只写字段含义和示例值，不写真实密钥
```

### 11.2 Docker 化 PostgreSQL + Neo4j

当前 PG 和 Neo4j 需要手动安装。用 Docker Compose 拉起这两个基础设施服务，降低环境搭建门槛。

### 11.3 拆分 web/server.py

1081 行的单文件已经严重影响可维护性。拆成 5 个路由文件，每个不超过 100 行。

### 11.4 建立跨领域评测集

当前评测只有红楼梦的 12 题。至少为三国演义也准备 10 题（项目已经切到三国演义域，`PROJECT_DOMAIN=三国演义`）。

---

## 12. 最终系统架构

### 第一层：文档处理层

- MinerU PDF 解析
- jieba 分词 + 语义边界切块
- 图片提取 + 结构识别

### 第二层：知识层

- pgvector 向量存储（bge-m3 768d）
- Neo4j 图谱存储（实体 + 关系 + 属性）
- Wiki 百科存储（结构化人物/事项卡片）
- 全文关键词索引

### 第三层：检索层

- 策略路由器（根据问题类型选择检索路径）
- 五路并行检索（关键词 / 向量 / KG / Wiki / Neo4j）
- 结果融合与重排序
- 缓存层（Redis）

### 第四层：Agent 层

- 工具注册表 + 可插拔工具
- 结构化 function calling
- ReAct 推理循环
- 思考链可视化数据生成

### 第五层：应用层

- 用户聊天界面（SSE 流式）
- 管理后台（监控/日志/配置/评测）
- 领域管理（上传/建库/上线）
- 评测与回归

### 第六层：基础设施层

- Docker Compose 编排
- PostgreSQL 18 + pgvector
- Neo4j 5.x
- Redis（缓存）
- Ollama（本地模型）
- Nginx（生产反代）
- GitLab CI

---

## 13. 与 kg-doc-pipeline 的对比与互补

| 维度 | kg-doc-pipeline | RedDream |
|------|----------------|----------|
| **定位** | 市场监管文档知识处理 | 古典文学智能问答 |
| **文档类型** | DOCX/PDF/图片（混合） | PDF（单一） |
| **核心能力** | 文档解析 + 知识图谱生成 | KG+向量融合检索 + Agent 问答 |
| **前端** | Vue3 + D3.js（pure-admin） | 原生 HTML + ECharts（内嵌 FastAPI） |
| **图谱存储** | PostgreSQL（JSONB 关系表） | Neo4j（原生图数据库） |
| **Agent** | 计划中（阶段五） | 已实现 ReAct 循环 |
| **多领域** | 不适用（单领域平台） | 四领域热切换 |
| **评测** | 无 | 12 题评测集 + LLM 裁判 |
| **Docker** | 完整编排 | 无 |

两个项目可以互相学习：

- kg-doc-pipeline 的 **工程化实践**（Docker、CI、前端分离、代码分层）可以移植到 RedDream
- RedDream 的 **检索架构**（五路融合 + 策略路由 + Neo4j）和 **Agent 设计** 可以移植到 kg-doc-pipeline

---

## 14. 一句话总结

> RedDream 的核心引擎已经跑通了，现在要做的是：配上方向盘和刹车（工程化）、换上更好的发动机（KG 质量）、装上导航系统（Agent），最后让它能带着任何乘客出发（通用化平台）。
