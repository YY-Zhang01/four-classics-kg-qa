# 四大名著知识图谱与智能问答系统 · 依赖清单（版本已锁定，确保可复现）
# MinerU 作为外部 CLI 调用（系统已装），不绑进本 venv，避免拖重环境。

# ── 大模型接入 ──
httpx==0.28.1             # OpenAI 兼容 HTTP 接口（网关 / 本地 Ollama）

# ── 配置管理 ──
python-dotenv==1.2.2      # 从 .env 读取配置，密钥不进代码

# ── 检索 ──
jieba==0.42.1             # 中文分词（P0 关键词检索）
numpy==2.5.1              # 向量余弦相似度计算

# ── 数据库 ──
psycopg2-binary==2.9.12   # PostgreSQL 驱动（向量库 + 三元组 + 审计日志）
neo4j==4.4.13             # Neo4j 驱动（可选，未配置时自动降级）

# ── 认证 ──
PyJWT==2.13.0             # JWT 签发/校验（auth 模块）
bcrypt==5.0.0             # 密码哈希（auth 模块）

# ── Web 服务 ──
fastapi==0.141.1          # Web 框架
uvicorn==0.52.0           # ASGI 服务器

# ── 测试 ──
pytest==9.1.1             # 测试框架
