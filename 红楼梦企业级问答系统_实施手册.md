# 红楼梦企业级智能问答系统 · 全流程实施手册

> 这是一份"边做边勾"的实施手册。目标：用红楼梦这个零合规风险、你有底子的题材，
> 亲手做一个**企业级**（不是玩具）的 RAG + 知识图谱问答系统，把公司政务项目要的
> 全套工程能力练到位。domain 是红楼梦，技能 100% 可迁移到政务项目。

---

## 0. 项目定位与验收总纲

**一句话**：喂它红楼梦原著 PDF，它能回答人物、情节、关系、诗词类问题，
答案**有原文出处、答不出就老实说、能追责**。

**三条红线**（一切技术选型都为它服务）：
1. **可溯源** —— 每个结论都能点回原著第几回、哪一段
2. **不编造** —— 检索不到就回"未找到"，绝不让大模型硬编
3. **可审计** —— 谁问了什么、答了什么、引了哪些出处，全程留痕

**它比毕设"企业"在哪**（这就是要练的升级点）：
| 维度 | 毕设旧版 | 企业级新版 |
|---|---|---|
| 数据来源 | 现成整理好的文本 | 自己爬 PDF + MinerU 解析 |
| 检索 | 只有 KG 查询 | RAG 向量检索 + KG 融合 |
| 抽取 | LTP 依存句法 | 大模型抽三元组 |
| 工程 | 能跑就行 | 质检 / 增量更新 / 评测 / 溯源 / 审计 |

---

## 1. 定案技术选型（拍板版）

| 层 | 定案 | 一句话理由 |
|---|---|---|
| 大模型 | 公司 AI 网关（开发）→ 本地私有化（交付） | 合规 + 免运维 + 可切换 |
| 模型接入 | 统一抽象接口，可插拔 | 换模型只改一处 |
| 主问答引擎 | RAG（向量检索）优先 | 覆盖八成问题，快见价值 |
| 知识图谱 | Neo4j，增强层，二期上 | 吃关系/多跳/大屏，你的主场 |
| 向量库 | PostgreSQL + pgvector | 复用本地已装的 PG18，能长大 |
| 元数据/审计 | PostgreSQL（同一套） | 少维护一个系统 |
| 文档解析 | MinerU | 岗位主攻方向 |
| 后端 / 前端 | Django REST + Vue | 复用毕设栈 |
| 部署 | Docker + Nginx，内网私有化 | 政务标配 |

> **MinerU 防放水提醒**：红楼梦原著 PDF 太干净，练不到解析硬仗。
> 要么专门找**影印/扫描版**红楼梦练 OCR，要么留几份**政务扫描件**专攻解析层。

---

## 2. 总体架构（七层）

```
⑦ 前端应用层    问答界面 / 出处卡片 / 关系图 / 管理后台 / 数据大屏
⑥ 服务API层     问答API · 会话管理 · 权限 · 限流 · 审计日志
⑤ 检索推理层    查询理解 → KG查询+向量召回 → 重排 → LLM受控生成   ★灵魂
④ 知识构建层    大模型抽三元组 → Neo4j图谱  +  文本块向量化 → pgvector
③ 数据治理层    清洗 · 语义切块 · 去重 · 质检 · 元数据 · 版本管理
② 文档解析层    MinerU: PDF/扫描件 → 结构化 Markdown/JSON
① 数据采集层    爬虫下载红楼梦PDF · 增量调度
   存储: Neo4j(图) + PostgreSQL(向量pgvector + 元数据 + 审计)
```

---

## 3. 分阶段路线图总览

| 阶段 | 目标 | 交付物 | 验收标准 |
|---|---|---|---|
| **P0** | 单文档跑通"解析→切块→纯RAG问答" | CLI 脚本 | 能答、答案带出处 |
| **P1** | 全流程Web化 + 向量检索 + 多文档 | Web问答界面 | 准确率 > 70% |
| **P2** | 大模型抽三元组入Neo4j + KG/RAG融合 | 关系图 + 融合问答 | 关系/多跳类问题能答 |
| **P3** | 质检+增量更新+评测+权限+审计 | 完整管理后台 | 评测指标达标、可回归 |
| **P4** | 私有化部署+监控+并发+本地模型 | 可交付部署包 | 压测通过、可离线运行 |

> **节奏**：P0~P1 你自己能啃；P2 复用毕设经验；P3~P4 是真正的大工程，
> 慢慢来。**但架构从 P0 就按产品设计，不返工。**

---

## 4. 前置环境准备（开工前一次搞定）

放 E 盘，不碰 C 盘。建议项目根目录：`E:\hlm_qa\`

- [ ] 建项目目录骨架（见第 9 节）
- [ ] Python 虚拟环境（隔离依赖）
- [ ] PostgreSQL 18（已装）里装 **pgvector 插件**，建库 `hlm_qa`
- [ ] Neo4j（P2 才用，可先不装）
- [ ] MinerU 跑通（已在 CPU 模式验证过）
- [ ] 模型接入：把公司 AI 网关地址/密钥写进**配置文件**（不硬编码、不进 git）

---

## 5. 逐阶段任务拆分（核心）

> 每张"任务卡"格式：**做什么 / 为什么 / 产出 / 新技能 / 验收**

### ▉ P0：单文档纯 RAG 跑通（CLI，2~3 个整块时间）

**P0-1 挑一份红楼梦 PDF**
- 做什么：找一版排版规整的红楼梦全文 PDF（先别挑扫描版），放 `pdfs\`
- 为什么：P0 只求跑通链路，不啃难关
- 产出：一份 PDF
- 验收：文件到位

**P0-2 MinerU 解析成 Markdown**
- 做什么：MinerU 把 PDF 转成 md，输出到 `parsed\`
- 新技能：MinerU 调用、参数、输出结构
- 产出：一个 md 文件
- 验收：肉眼对照原文，内容基本对得上

**P0-3 语义切块**
- 做什么：写 `scripts/split.py`，把 md 按**回目/段落**切成小块
- 为什么：整本书塞不进大模型，用时只挑相关几块；切太碎会断句义
- 产出：`chunks/*.json`，每块含 `{id, text, source, 回目, chunk_no}`
- 新技能：chunking（切块）、为每块打元数据
- 验收：随手翻几块，句子完整、带得上出处

**P0-4 先用关键词检索（土办法先跑通）**
- 做什么：写 `scripts/search.py`，输入问题，挑出最相关 3~5 块
- 为什么：**先别上向量**，用关键词匹配跑通，亲眼看"检索"在干嘛
- 产出：输入问题 → 打印命中的块
- 验收：问"黛玉葬花"，能捞出相关段落

**P0-5 拼提示词 + 调大模型（受控生成）**
- 做什么：写 `scripts/ask.py`，提示词强约束"只据资料答、标出处、答不出就说没有"
- 为什么：这是 RAG 治幻觉的核心，也是毕设 DeepSeek 对话的升级
- 产出：大模型据检索内容作答 + 出处
- 新技能：受控提示词、流式输出
- 验收：答案确实来自那几块，末尾有"出处：第X回"

**P0-6 串成一条龙**
- 做什么：`scripts/main.py` 把 P0-3→4→5 串起来
- 产出：一个命令行问答小程序
- 验收：连问三五个问题，答得准、出处对

> **P0 交付**：喂一份红楼梦 PDF，命令行能有理有据地问答。RAG 全流程走通。

---

### ▉ P1：单机 Web 版 + 向量检索 + 多文档（1~2 周）

**P1-1 pgvector 建表**
- 做什么：在 PG 建 `chunks` 表（含 `embedding vector` 字段）+ 元数据字段
- 新技能：pgvector、向量字段、相似度索引
- 验收：能插入一条带向量的记录并按相似度查出来

**P1-2 文本块向量化（embedding）**
- 做什么：写脚本把每块文本转成向量，存进 pgvector
- 为什么：向量检索能理解"语义相近"，比关键词强得多
- 新技能：embedding 模型调用、批量入库
- 验收：查询向量能召回语义相关块（问"林妹妹"能召回"黛玉"相关块）

**P1-3 检索从关键词升级为向量召回**
- 做什么：把 P0-4 的关键词匹配换成向量相似度 Top-K
- 验收：召回质量肉眼可见变好

**P1-4 多文档入库 + 元数据规范**
- 做什么：把流程从"1 份"扩到"多份"，每块严格带来源
- 验收：跨多份文档都能正确检索、正确标出处

**P1-5 Django REST 包一层问答 API**
- 做什么：把 CLI 逻辑搬进 Django（复用毕设后端骨架，但当新知识学）
- 新技能：REST 接口、流式响应（SSE）
- 验收：POST 一个问题，返回流式答案 + 出处

**P1-6 Vue 问答界面 + 出处卡片**
- 做什么：前端做问答框、流式显示、答案下方挂**可点开的出处卡片**
- 为什么：出处可点回原文 = 信任的关键
- 验收：网页上问答、能看到出处

> **P1 交付**：一个能用浏览器访问的红楼梦问答网站，向量检索、多文档、带出处。
> 自测准确率 > 70%。

---

### ▉ P2：知识图谱融合（复用毕设主场，1~2 周）

**P2-1 定红楼梦 KG Schema**
- 节点：人物、家族、事件、地点、诗词
- 关系：亲属（父/母/兄弟/夫妻…）、主仆、暧昧、参与事件、居住…
- 验收：Schema 文档 + 关系白名单

**P2-2 大模型抽三元组（替代 LTP）**
- 做什么：给大模型一段原文 + Schema，让它吐三元组 JSON
- 为什么：比毕设的 LTP 依存句法准，也是公司做法
- 新技能：抽取提示词、JSON 结构化输出、抽取结果校验
- 验收：抽出的三元组人工抽检准确率达标

**P2-3 三元组入 Neo4j**
- 做什么：校验（白名单卡关系）后写入 Neo4j（复用毕设建模经验）
- 验收：Neo4j 里能看到人物关系网

**P2-4 KG + RAG 融合检索（灵魂，见第 7 节伪代码）**
- 做什么：查询理解 → KG 召回结构化事实 + 向量召回原文 → 重排 → 一起喂大模型
- 新技能：双路召回、重排、上下文拼装
- 验收：关系/多跳问题（"宝玉的表妹是谁""贾母和黛玉什么关系"）能答对

**P2-5 关系图可视化**
- 做什么：ECharts 力导向图（复用毕设那套）
- 验收：答案配一张关系子图

> **P2 交付**：融合问答——内容型走 RAG，关系型走 KG，大模型统一组织答案 + 关系图。

---

### ▉ P3：产品化（大工程开始，边学边做）

- **P3-1 数据质检关**：解析结果抽检打分，不合格打回；OCR 准确率抽样核对
- **P3-2 增量更新**：记录每份文件 URL+哈希，只处理新增/变更；旧版本标失效不删
- **P3-3 评测体系**：见第 8 节，建问题集 + 量化指标，每次改动回归
- **P3-4 权限与审计**：登录、角色、全链路审计日志（谁问了什么、引了什么）
- **P3-5 管理后台**：文档管理、图谱 CRUD、数据大屏（高频问题、答不出的问题）

> **P3 交付**：一个有质量保障、能持续更新、能证明"它准"的系统。

---

### ▉ P4：上线级（私有化交付，远期）

- **P4-1 本地模型部署**：把模型接入切到本地部署（3060 先验证，生产换强卡）
- **P4-2 Docker 化 + Nginx 反代**
- **P4-3 监控**：日志分级、关键链路可追踪、异常告警
- **P4-4 并发与容错**：限流、压测、超时兜底

> **P4 交付**：可离线私有化部署的完整交付包，压测通过。

---

## 6. 数据模型细化

**PostgreSQL（pgvector）—— 文本块表 `chunks`**
```
id            主键
doc_id        所属文档
source_file   来源文件名
chapter       回目（如"第二十三回"）
chunk_no      块序号
content       原文文本
embedding     向量（vector 类型）
create_time / version   版本与时间
```

**PostgreSQL —— 元数据/治理表**
```
documents     文档表（文件名、来源URL、哈希、解析质量分、生效状态、版本）
audit_logs    审计表（用户、问题、答案、引用块ID、时间、耗时）
```

**Neo4j —— 知识图谱（P2）**
```
(:Person {name, family})
(:Family {name})
(:Event {name, chapter})
关系：[:亲属 {type:"父亲"}] [:主仆] [:参与] [:居住] ...
```

---

## 7. 检索推理层伪代码（融合策略，P2 灵魂）

```
def answer(question):
    # 1. 查询理解：抽实体 + 判断问题类型
    entities, qtype = understand(question)

    # 2. 双路召回
    kg_facts = []
    if qtype == "关系型" and entities:
        kg_facts = query_neo4j(entities)      # 顺关系链拿结构化事实
    vec_chunks = vector_search(question, top_k=5)  # 向量召回原文块

    # 3. 空结果保护（不编造红线）
    if not kg_facts and not vec_chunks:
        return "未找到相关内容", []

    # 4. 重排 + 拼上下文（结构化事实在前，原文在后，控长度）
    context = build_context(kg_facts, vec_chunks)

    # 5. 受控生成
    prompt = f"只根据以下资料回答，答不出就说没有，每个结论标出处。\n{context}\n问题：{question}"
    answer = llm_stream(prompt)

    # 6. 溯源回填 + 写审计日志
    sources = map_back_to_sources(vec_chunks)
    log_audit(question, answer, sources)
    return answer, sources
```

---

## 8. 评测体系（证明"它准"）

- **测试问题集**：人工准备 50~100 个真实问题 + 标准答案（覆盖内容型/关系型/多跳/该拒答的）
- **量化指标**：
  - 答案准确率
  - **溯源正确率**（出处是否真对应）—— 最重要
  - 召回命中率
  - 响应延迟 P95
  - 拒答正确率（该说没有时有没有老实拒答）
- **回归**：每次改代码/换模型，重跑问题集，指标不许倒退

---

## 9. 建议目录结构（代码分层）

```
E:\hlm_qa\
├── pdfs\              原始 PDF
├── parsed\            MinerU 解析结果
├── chunks\            切块中间产物
├── config\            配置（模型密钥、DB地址，不进git）
├── crawler\           ① 采集
├── parser\            ② MinerU 解析封装
├── governance\        ③ 清洗/切块/去重/质检
├── kb_builder\        ④ 抽三元组 + 向量化
├── retrieval\         ⑤ 检索推理（融合核心）
├── llm\               模型接入层（可插拔接口）★
├── backend_django\    ⑥ API（P1 起）
├── frontend_vue\      ⑦ 前端（P1 起）
├── eval\              评测问题集 + 脚本
└── scripts\           P0 阶段的独立脚本
```

> **模型接入层单独成模块**是"不返工"的命门：换模型只改这里。

---

## 10. 每阶段新技能清单（学习地图）

| 阶段 | 要学的新概念 |
|---|---|
| P0 | MinerU、切块、RAG 基本原理、受控提示词 |
| P1 | embedding、向量检索、pgvector、REST API、SSE 流式、Vue 前端 |
| P2 | 大模型信息抽取、KG Schema 设计、Neo4j/Cypher（复习）、双路召回与融合 |
| P3 | 数据质检、增量更新、评测指标、权限、审计 |
| P4 | 本地模型部署、Docker、Nginx、监控、压测 |

---

## 11. 里程碑自检

- [ ] P0：CLI 能问答、带出处
- [ ] P1：Web 版、向量检索、多文档、准确率 > 70%
- [ ] P2：KG 融合、关系/多跳能答、关系图
- [ ] P3：质检+增量+评测+权限+审计
- [ ] P4：私有化部署+监控+压测通过

---

> 下一步：从 **P0-1** 开始，一步步来。你实操、我陪跑，每一行讲清"在干嘛、为什么"。

---

## 12. 各阶段 Codex 指令模板（复制即用）

> 用法：先贴一次 **12.0 通用约定**给 Codex，之后每阶段贴对应指令。
> 一次只喂一个阶段，Codex 做完你验收通过，再喂下一个。报错就把完整报错原样回贴给它。

### 12.0 通用约定（每次开新会话先贴这段）

```
你是本项目的代码实现者。全程遵守以下约定：
1. 项目根目录 E:\hlm_qa\，所有文件放 E 盘，绝不碰 C 盘。
2. 我的终端是 Windows PowerShell，命令用分号 ; 分隔，不要用 &&。
3. 所有密钥、数据库地址、模型配置放 config\ 下的配置文件，通过环境变量或配置读取，
   绝不硬编码进代码，绝不提交进 git（写好 .gitignore）。
4. 大模型调用统一封装到 llm\ 模块的可插拔接口，业务代码只调这个接口，不直接写死某家 API。
5. 代码风格：中文注释、函数职责单一、关键步骤有日志；不要过度设计，够用就好。
6. 每完成一个可运行的小单元就停下来告诉我怎么验收，等我确认再继续。
7. 产品三条红线：答案可溯源、检索不到不许编造、关键操作留审计日志。
```

### 12.1 P0 指令 —— 单文档纯 RAG 命令行

```
任务：搭一个命令行版红楼梦问答，跑通"解析→切块→检索→大模型作答"全链路。
分步交付，每步可单独验收：
1. 建项目骨架和 Python 虚拟环境，装依赖，写 .gitignore。
2. scripts/split.py：读 parsed\ 下的红楼梦 markdown，按回目/段落切块，
   每块存成 chunks\*.json，字段 {id, text, source, chapter, chunk_no}。
3. scripts/search.py：输入一个问题，用关键词匹配从块里挑最相关的 3~5 块并打印
   （这一步先不要上向量检索）。
4. llm/ 下写模型接入层：封装成 chat(messages) 接口，底层调公司 AI 网关（配置读取）。
5. scripts/ask.py：把检索到的块拼进提示词，强约束"只根据资料回答、答不出说没有、
   每个结论标出处（第X回）"，调 llm 流式输出。
6. scripts/main.py：把 3→5 串成一条龙，命令行循环问答。
验收：我喂一份红楼梦 md，命令行能问答，答案来自检索内容且带出处。
```

### 12.2 P1 指令 —— 向量检索 + Web 化 + 多文档

```
任务：把 P0 的命令行版升级成 Web 问答系统，检索换成向量检索，支持多文档。
分步交付：
1. 在本地 PostgreSQL 18 装 pgvector 插件，建库 hlm_qa，建 chunks 表
   （含 embedding vector 字段 + source_file/chapter/chunk_no 等元数据）和 documents 表。
2. kb_builder/embed.py：把每个文本块转成向量写入 pgvector（embedding 模型走配置）。
3. retrieval/：把关键词检索换成向量相似度 Top-K 召回。
4. 支持批量导入多份文档，每块严格记录来源，保证出处可回溯。
5. backend_django/：用 Django REST 暴露问答接口，流式返回（SSE），复用 retrieval 和 llm 模块。
6. frontend_vue/：做问答界面，流式显示答案，答案下方展示可点开的出处卡片。
验收：浏览器访问，多文档下问答准确、出处正确，自测准确率 > 70%。
```

### 12.3 P2 指令 —— 大模型抽三元组 + Neo4j + KG/RAG 融合

```
任务：加入知识图谱，做 KG + RAG 融合问答。
分步交付：
1. 定义红楼梦 KG Schema（节点：人物/家族/事件/地点；关系：亲属/主仆/参与等）+ 关系白名单。
2. kb_builder/extract.py：给大模型一段原文 + Schema，让它输出三元组 JSON，
   并做校验（关系必须在白名单内，非法丢弃）。
3. 校验后的三元组写入 Neo4j。
4. retrieval/ 升级为融合检索：查询理解判断问题类型 →
   关系型走 Neo4j 拿结构化事实 + 向量召回原文块 → 重排 → 拼上下文喂大模型。
   检索全空时返回"未找到"，不许编造。
5. 前端答案配一张 ECharts 关系子图。
验收：关系/多跳类问题（如"宝玉的表妹是谁"）能答对，并给出关系图。
```

### 12.4 P3 指令 —— 产品化（质检/增量/评测/权限/审计）

```
任务：把系统做成有质量保障、能持续更新、能评测的产品。分模块交付：
1. 数据质检：解析结果自动打分 + 人工复核入口，不合格不入库。
2. 增量更新：记录每份文件 URL+哈希，只处理新增/变更；政策旧版本标失效不删、可回溯。
3. 评测：建 50~100 条问题集（含标准答案和该拒答的），
   脚本自动算准确率/溯源正确率/召回率/延迟/拒答正确率，支持一键回归。
4. 权限：登录 + 角色（游客/工作人员/管理员）。
5. 审计：每次问答记录用户/问题/答案/引用块ID/耗时到 audit_logs。
6. 管理后台：文档管理、图谱 CRUD、数据大屏（高频问题、答不出的问题）。
验收：评测指标可量化、可回归；后台可管理数据。
```

### 12.5 P4 指令 —— 私有化部署上线

```
任务：做成可离线私有化部署的交付包。分模块交付：
1. 模型接入层增加"本地部署模型"实现，配置切换即可从网关切到本地（先在 3060 上验证）。
2. 用 Docker 打包后端/前端/数据库，写 docker-compose 一键起。
3. Nginx 反向代理 + 静态资源托管。
4. 日志分级 + 关键链路可追踪 + 异常告警。
5. 接口限流、超时兜底，做一次并发压测并给报告。
验收：断外网也能跑，压测通过。
```

## 12.6 Agent 化指令（P2 之后，把固定管线升级为 Agent 编排）

```
任务：把固定管线 RAG 升级为 Agent 编排式问答。
1. 把已有能力封装成工具：vector_search(query)、kg_query(entities)、（可选）sql_query，
   每个工具有明确的名字、说明、入参出参 schema，注册到一个工具表。
2. 写 Agent 主循环：用大模型的 function calling，让模型根据问题自己决定调哪个工具、
   调几次，信息够了再给最终答案；检索全空不许编造，回"未找到"。
3. 每一步决策（调用的工具/参数/结果）写入日志，支持事后回放。
4. 提示词单独放配置文件，与代码分离。
5. 保持工具契约稳定：新增能力只加新工具、注册进表，不改主循环。
验收：问一个需要"先查图谱再查原文"的复合问题，Agent 能自己分两步调工具答对，
      且日志能看到它的决策轨迹。
```

### 使用提示

- **一次一阶段**，别把 P0~P4 一次性全甩给它，Codex 会顾此失彼。
- 每个阶段内部若太大，就照指令里的"分步交付"编号，**一步一验收**。
- 遇到报错：**原样回贴完整报错**给 Codex，别自己转述。
- Codex 写完的关键代码，回来找我，我给你讲"它写了啥、为什么这么写"——保证你是懂的，不是只会复制。

---

## 13. 细颗粒度执行清单（WBS·可打勾）

> 每个任务小到"半天内能做完、能单独验收"。格式：`编号 动作｜产出｜验收｜耗时`。
> 做完一个勾一个，产出和验收对上了再走下一个。

### 阶段 P0：单文档纯 RAG 命令行（对应 Codex 指令 12.1）

**子阶段 A·环境与骨架**
- [ ] P0.1 建目录骨架（E:\hlm_qa\ 及各子目录）｜空目录树｜目录齐全｜15分
- [ ] P0.2 建 Python 虚拟环境并激活｜.venv｜能激活｜10分
- [ ] P0.3 装依赖 + 生成 requirements.txt｜依赖清单｜pip list 能看到｜15分
- [ ] P0.4 写 config\ 配置（网关地址/密钥）+ .gitignore｜配置文件｜密钥不进 git｜20分

**子阶段 B·数据就位**
- [ ] P0.5 找一份排版规整的红楼梦 PDF 放 pdfs\｜PDF｜文件到位｜10分
- [ ] P0.6 MinerU 解析 PDF → parsed\ 的 md｜md 文件｜肉眼看对得上原文｜30分

**子阶段 C·切块**
- [ ] P0.7 写 split.py，按回目/段落切块 → chunks\*.json｜JSON 块｜字段齐、句子完整｜半天
- [ ] P0.8 抽查几块切块质量｜—｜块不断句、带得上出处｜15分

**子阶段 D·检索（先土办法）**
- [ ] P0.9 写 search.py，关键词匹配挑 Top 3~5 块｜检索函数｜问"黛玉葬花"能捞相关块｜半天

**子阶段 E·接大模型**
- [ ] P0.10 写 llm/ 接入层 chat(messages)｜可插拔接口｜代码只调这一层｜半天
- [ ] P0.11 测 llm 连通（问一句 hi）｜—｜能收到回复｜15分
- [ ] P0.12 写 ask.py，受控提示词 + 流式输出｜作答函数｜答案带出处、答不出会说没有｜半天

**子阶段 F·串联**
- [ ] P0.13 写 main.py 串成一条龙｜命令行程序｜能循环问答｜2小时
- [ ] P0.14 端到端自测 5 个问题｜—｜答得准、出处对｜半天

> ✅ **P0 完成标志**：命令行能有理有据问答，答案带出处。

### 阶段 P1：向量检索 + Web 化（对应 12.2）

**A·向量库**
- [ ] P1.1 PG18 装 pgvector 插件｜—｜CREATE EXTENSION 成功｜30分
- [ ] P1.2 建库 hlm_qa + chunks 表（含 vector 字段）+ documents 表｜建表 SQL｜表结构正确｜1小时
- [ ] P1.3 写一条带向量的测试数据并按相似度查出｜—｜能插能查｜30分

**B·向量化与检索升级**
- [ ] P1.4 写 embed.py，文本块→向量→写入 pgvector｜入库脚本｜块都有向量｜半天
- [ ] P1.5 检索换成向量 Top-K 召回｜retrieval 模块｜问"林妹妹"能回"黛玉"块｜半天
- [ ] P1.6 多文档批量入库 + 来源记录｜—｜跨文档出处正确｜半天

**C·Web 化**
- [ ] P1.7 Django REST 问答接口（SSE 流式）｜API｜POST 问题返回流式答｜1天
- [ ] P1.8 Vue 问答界面 + 流式显示｜页面｜网页能问答｜1天
- [ ] P1.9 答案下挂可点开的出处卡片｜—｜点出处能看原文｜半天
- [ ] P1.10 自测准确率（手准 20 题）｜—｜> 70%｜半天

> ✅ **P1 完成标志**：浏览器问答、向量检索、多文档、带出处，准确率 > 70%。

### 阶段 P2：知识图谱融合（对应 12.3）

- [ ] P2.1 定 KG Schema + 关系白名单｜Schema 文档｜评审通过｜半天
- [ ] P2.2 写 extract.py：大模型抽三元组 JSON｜抽取脚本｜单段能抽出三元组｜1天
- [ ] P2.3 三元组校验（白名单卡）｜校验逻辑｜非法关系被丢弃｜半天
- [ ] P2.4 三元组写入 Neo4j｜建图脚本｜Neo4j 里有关系网｜半天
- [ ] P2.5 查询理解：判断关系型/内容型｜路由逻辑｜分流正确｜半天
- [ ] P2.6 融合检索：KG 事实 + 向量原文 → 重排 → 喂模型｜融合逻辑｜关系题能答对｜1天
- [ ] P2.7 空结果保护（不编造）｜—｜查不到回"未找到"｜2小时
- [ ] P2.8 前端 ECharts 关系子图｜可视化｜答案配图｜半天

> ✅ **P2 完成标志**：关系/多跳问题能答对并给关系图。

### 阶段 P3：产品化（对应 12.4，每项可独立推进）

- [ ] P3.1 解析质检：自动打分 + 人工复核入口
- [ ] P3.2 增量更新：URL+哈希去重、旧版本标失效
- [ ] P3.3 评测集：50~100 题 + 标准答案
- [ ] P3.4 评测脚本：准确率/溯源率/召回率/延迟/拒答率 + 一键回归
- [ ] P3.5 权限：登录 + 三角色
- [ ] P3.6 审计：audit_logs 全链路记录
- [ ] P3.7 管理后台：文档管理 + 图谱 CRUD + 数据大屏

> ✅ **P3 完成标志**：指标可量化可回归，后台可管理。

### 阶段 P4：私有化部署（对应 12.5，远期）

- [ ] P4.1 本地部署模型实现 + 配置切换（3060 验证）
- [ ] P4.2 Docker 打包 + docker-compose 一键起
- [ ] P4.3 Nginx 反代 + 静态托管
- [ ] P4.4 日志分级 + 链路追踪 + 告警
- [ ] P4.5 限流/兜底 + 并发压测报告

> ✅ **P4 完成标志**：断外网能跑，压测通过。

---

### 节奏建议

- **P0 目标：2~3 个整块时间启动**，先把全链路跑通，别憋在完美。
- 每天开工前看一眼本清单，选一条未勾的小任务，喂对应 Codex 指令，做完验收打勾。
- 卡住超过 30 分钟不要硬刚，拿着具体任务编号来找我。

---

## 14. 升级为 Agent 智能体 + 可维护性设计

> 你的目标：不只是"固定流程问答机"，而是能自己思考、自己调度的**智能体**，还要好维护。
> 这一节讲清楚怎么从固定管线平滑长成 Agent，以及维护性怎么从一开始就设计好。

### 14.1 两种架构的区别

- **固定管线 RAG（P0/P1 先做）**：每个问题都走死流程"检索→拼接→回答"，
  用不用检索、用哪个检索，是代码写死的。简单、可控、好入门。
- **Agent 编排（目标）**：大模型当"大脑/调度员"，把"查向量库""查知识图谱""查数据库""算数"
  都做成**工具(tool)**。大模型根据问题**自己决定**调哪个工具、调几次、要不要追问，
  凑够信息才给最终答案。这就是你学过的 function calling + 循环。

比喻：固定管线像"填表机器"，一套流程走到底；Agent 像"失忆但聪明的调度员"，
手里一份工具菜单，边想边点菜，直到凑齐答案。

### 14.2 Agent 版架构

```
用户问题
  ↓
┌───────────────────────────────────┐
│  Agent 主循环 (loop)                 │
│  大模型(大脑) + 工具菜单 + history      │
│    while 还没给最终答案:               │
│      模型看问题和已拿到的信息 →          │
│      决定：调某个工具 还是 给答案         │
│      若调工具 → 执行 → 结果塞回 history  │
└───────────────────────────────────┘
  工具箱: [向量检索] [图谱查询] [SQL查询] [计算/其他]
  ↓
最终答案 + 出处 + 决策轨迹(日志)
```

### 14.3 别一上来就 Agent（升级路线）

- **P0/P1**：先用固定管线，把每个"工具"（向量检索、图谱查询）本体做出来、跑通。
- **P2**：把这些能力**注册成工具**，写 Agent 主循环，让大模型用 function calling 自己调度。
- 好处：工具本体你 P0/P1 已经做好，Agent 化只是"换个调度方式"，不会开局就淹在复杂度里。

### 14.4 可维护性设计（你的另一个明确目标）

1. **工具即插件**：每个能力封装成标准工具（统一入参出参 schema），注册到工具表。
   加新数据源 = 加新工具，**主循环一行不改**。
2. **Prompt 与代码分离**：所有提示词放独立配置文件，调 prompt 不动代码。
3. **模型可插拔**：已有的 llm/ 接入层，换模型改一处。
4. **决策留痕（Agent 最关键）**：每一步"调了什么工具、什么参数、拿到什么"都记日志。
   Agent 最难维护的是"它为什么这么答"，能回放决策才好排查。
5. **分层 + 配置化 + 评测回归**：架构分层清晰、参数走配置、每次改动跑评测集不倒退。
6. **工具契约稳定**：工具的入参出参格式固定，内部实现随便改，不影响调用方。

> ✅ **Agent 化完成标志**：问一个复合问题（要先查图谱再查原文），
> Agent 能自己分步调工具答对，且日志能看到它的完整决策轨迹。
